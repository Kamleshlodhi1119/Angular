import { SnowCursorDirective } from './snow-cursor.directive';

describe('SnowCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new SnowCursorDirective();
    expect(directive).toBeTruthy();
  });
});
